name: PR-Agent AI Code Review

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

jobs:
  pr-agent:
    uses: KiarashMinoo/.github/.github/workflows/pr-agent.yml@main
    secrets: inherit

